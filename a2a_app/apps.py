"""Django app configuration for a2a_app."""

from django.apps import AppConfig


class A2AAppConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "a2a_app"
    verbose_name = "A2A Router"
